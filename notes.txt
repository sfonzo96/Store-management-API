MODELO --> SERVICIO / DAO (instancia de CRUD) --> REPOSITORY -- Obtiene resultado del DAO --> DTO (mapea y responde) --> CONTROLLER

//

CHAT GTP y otras fuentes indican inversión entre repository y servicio, aludiendo que repository no tiene que conocer nada sobre la lógica de negocios, por ende servicio consume repository y repository no consume servicio.

//

No se va a aplicar factory porque no se ve necesidad de cambiar la persistencia de archivos (verificar que no sea requisito para entrega final)

